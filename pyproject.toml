[project]
name = "GBEasy"
version = "0.1.0"
description = "Generate config and apply Goldberg Emulator on a given steam app"
readme = "README.md"
license.file = "LICENSE"
requires-python = ">=3.13"
dependencies = ["pyinstaller>=6.13.0", "requests>=2.32.3"]
